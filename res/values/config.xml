<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Define Device Lid Style

     1 HTC Style Dotcase
     2 Asus/LG Style Circle Window Cover

     For example, a device with Asus Circle Cover would set 2 -->
    <integer name="config_deviceCoverType">0</integer>

    <!-- Define main circle parameters -->
    <integer name="x_offset" translatable="false">0</integer>
    <integer name="y_offset" translatable="false">0</integer>
    <integer name="radius_offset" translatable="false">0</integer>

    <!-- Define Path to lid switch node. Node should report the following:
        OEPN = 0, Closed =1
        example path : "/sys/devices/virtual/switch/smartcover/state" -->
    <string name="cover_node" translatable="false">@null</string>

    <!-- Define path to lid Uevent node. Node should trigger on every cover change
         example path : "DEVPATH=/devices/virtual/switch/smartcover" -->
    <string name="cover_uevent_match" translatable="false">@null</string>

</resources>